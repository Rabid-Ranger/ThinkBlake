const http=require('node:http'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..'),source=require('../api/source');http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');if(url.pathname==='/api/source'){res.status=x=>(res.statusCode=x,res);res.send=x=>res.end(x);return source(req,res);}const file=url.pathname==='/'?'index.html':url.pathname.slice(1);if(!['index.html','favicon.svg'].includes(file)&&!/^analytics\/[\w-]+\.js$/.test(file)){res.statusCode=404;return res.end();}res.setHeader('Content-Type',file.endsWith('.js')?'application/javascript':file.endsWith('.svg')?'image/svg+xml':'text/html');res.setHeader('Cache-Control','no-store');res.end(fs.readFileSync(path.join(root,file)));}).listen(4882,'127.0.0.1',()=>console.log('Live analytics test http://127.0.0.1:4882'));
